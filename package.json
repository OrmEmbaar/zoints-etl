{
    "name": "@zoints/etl",
    "description": "ETL for Solana ZEE program transactions",
    "author": "OrmEmbaar",
    "version": "0.1.0",
    "license": "MIT",
    "scripts": {
        "start": "nodemon bin/index.ts start",
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rm -rf dist && mkdir dist && prisma generate && cp -r client dist/client && cp -r prisma dist/prisma && tsc",
        "build:check": "tsc --noEmit"
    },
    "dependencies": {
        "@prisma/client": "^3.3.0",
        "@zoints/staking": "^1.0.2",
        "bottleneck": "^2.19.5",
        "bs58": "^4.0.1",
        "commander": "^8.3.0",
        "prisma": "^3.3.0",
        "winston": "^3.3.3",
        "winston-slack-webhook-transport": "^2.1.0"
    },
    "devDependencies": {
        "@types/bs58": "^4.0.1",
        "@types/dotenv": "^8.2.0",
        "@types/node": "^16.10.9",
        "nodemon": "^2.0.13",
        "ts-node": "^10.3.0",
        "typescript": "^4.4.4"
    }
}
